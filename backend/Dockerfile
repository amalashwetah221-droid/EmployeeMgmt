# Use an official OpenJDK runtime as the base image
FROM openjdk:11-jdk-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the JAR file from the target folder (generated by Maven) into the /app directory in the container
COPY target/backend-app.jar /app/backend-app.jar

# Set the entry point for the container to run the JAR file
ENTRYPOINT ["java", "-jar", "backend-app.jar"]

# Expose port 8080 to access the application
EXPOSE 8080
